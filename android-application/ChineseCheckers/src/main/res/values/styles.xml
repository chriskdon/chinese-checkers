<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:textColor">@color/app_text</item>
        <item name="android:textSize">@dimen/activity_normal_text_size</item>

        <item name="android:buttonStyle">@style/Button</item>

        <!-- bug: the following breaks the keyboard; it doesn't pop up -->
        <!--<item name="android:editTextStyle">@style/EditText</item>-->
    </style>

    <style name="Button" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:textColor">@color/app_button_text</item>
        <item name="android:background">@drawable/chch_button</item>
    </style>

    <style name="AiDifficultyToggleButton" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:textColor">@color/app_background</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="CustomDialogTheme" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@color/app_background_translucent</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!--TODO: Remove once custom dialog is created-->
    <style name="Dialog" parent="android:Theme.Holo.Dialog">
    </style>

    <!-- the following is manually entered for each EditText due to above bug -->
    <style name="EditText" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:background">@drawable/chch_edittext</item>
        <item name="android:textColorHint">@color/app_hint_text</item>
        <item name="android:gravity">center_vertical|left</item>
        <item name="android:padding">5dp</item>
    </style>

</resources>
